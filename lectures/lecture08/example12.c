#include <stdio.h>

int main() {
    // Array declaration w/o initialization
    int arr[15];

    // Array declaration w/ initialization
    int arr2[] = {1,2,3};

    // Char array (string)
    char arr3[] = "hello";

    // Array element access
    int myvar = arr2[2];

    // Writing to an array element
    arr[11] = -5;
}